// Side-effect imports without own typings. Since TypeScript 6 an untyped side-effect import is an
// error (TS2882), so the modules are declared here as untyped.
declare module '*.css';
declare module 'moment/locale/*';
