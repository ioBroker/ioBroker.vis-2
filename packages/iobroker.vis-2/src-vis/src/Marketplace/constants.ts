/**
 * Widgeteria (marketplace) is currently switched off in the GUI.
 *
 * The whole implementation stays in place - all entry points are only hidden (`display: none` resp. `hide` for
 * menu items). Set this flag to `false` to bring the widgeteria back.
 */
export const WIDGETERIA_DISABLED = true;
